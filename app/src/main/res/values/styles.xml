<resources>
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
    </style>

    <style name="WeekdaySelector">
        <item name="android:layout_height">@dimen/weekday_selector_size</item>
        <item name="android:layout_width">@dimen/weekday_selector_size</item>
        <item name="android:layout_marginLeft">@dimen/weekday_selector_margin</item>
        <item name="android:layout_marginRight">@dimen/weekday_selector_margin</item>

        <item name="android:padding">@dimen/weekday_selector_padding</item>
        <item name="android:background">@drawable/weekday_background</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">15dp</item>
        <item name="android:clickable">true</item>
    </style>
</resources>
